services:
  - type: web
    name: gangotri-whatsapp-automation
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    autoDeploy: true
